info:
  name: SeedUsers
  type: http
  seq: 1

http:
  method: POST
  url: "{{API-URL}}/seeder/users"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
